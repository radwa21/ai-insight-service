services:
  mongodb:
    image: mongo:7.0
    container_name: ai-insight-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ai_insight_db
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: